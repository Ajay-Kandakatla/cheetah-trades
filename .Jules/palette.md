## 2026-06-13 - Added htmlFor to Watchlist search/sector labels
**Learning:** Found labels wrapping inputs with no direct semantic association. Added IDs and htmlFor for improved accessibility.
**Action:** When creating forms, make sure to link `label` elements to `input` elements via `htmlFor` and `id` attributes.
