fastapi==0.115.4
uvicorn[standard]==0.32.0
httpx==0.27.2
websockets==13.1
python-dotenv==1.0.1
pymongo==4.10.1
pandas==2.3.3
numpy==2.0.2
yfinance==1.2.0
requests==2.32.5
beautifulsoup4==4.14.3
# lxml — pandas.read_html uses it under the hood for the Wikipedia
# S&P 500 component fetcher (sepa/universe.py fetch_sp500). Without
# it, fetch falls all the way through to the 112-symbol curated list.
# Added 2026-05-21 when iShares CSV blocked direct downloads, forcing
# Wikipedia to become the primary public source.
lxml==5.3.0
pyarrow==21.0.0
pywebpush==2.0.0
cryptography>=41.0.0
# Playwright + stealth patches for the lifeboard.playwright_scraper module.
# stealth defeats most navigator-based bot fingerprinting; Chromium is
# installed in the Dockerfile after pip install (separate step).
playwright==1.49.1
playwright-stealth==1.0.6

pytest>=9.0
